{
  "name": "custom-orders-functions",
  "description": "Cloud Functions for Custom Orders — trigger emails on new orders",
  "main": "index.js",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
